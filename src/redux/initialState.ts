import TodoModel from "../Models/TodoModel";

export const todos: TodoModel[] = [
    new TodoModel('Buy groceries'),
    new TodoModel('Walk the dog'),
    new TodoModel('Finish homework'),
    new TodoModel('Read a book'),
    new TodoModel('Go for a run'),
    new TodoModel('Call mom'),
    new TodoModel('Write an email to boss'),
    new TodoModel('Clean the kitchen'),
    new TodoModel('Pay bills'),
    new TodoModel('Watch a movie')
];
